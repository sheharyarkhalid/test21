var module_85687268645 = function() {
    var __hs_messages = {};
    i18n_getmessage = function() {
        return hs_i18n_getMessage(__hs_messages, hsVars.language, arguments)
    }, i18n_getlanguage = function() {
        return hsVars.language
    };
    class FeatureFour {
        constructor(initElement) {
            this.initElement = initElement, this.lifecycle()
        }
        lifecycle() {
            this.createClickEvents(), this.setState(0)
        }
        createClickEvents() {
            this.initElement.querySelectorAll(".kl-features-04__content-list-item").forEach((element => {
                element.addEventListener("click", (event => {
                    this.setState(element.dataset.id)
                }))
            }))
        }
        setState(stateNumber) {
            this.initElement.querySelectorAll(".kl-features-04__content-list-item").forEach((element => {
                element.classList.remove("active"), element.dataset.id == stateNumber && element.classList.add("active")
            })), this.initElement.querySelectorAll(".kl-features-04__card-item").forEach((element => {
                element.classList.remove("active"), element.dataset.id == stateNumber && element.classList.add("active")
            }))
        }
    }
    window.addEventListener("load", (() => {
        document.querySelectorAll(".kl-features-04").forEach((element => {
            new FeatureFour(element)
        }))
    }))
}();